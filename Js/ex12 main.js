function checkRange(n) {
  if (n >= 100 && n <= 200) {
    console.log("The number is in the range");
  } else {
    console.log("Number is not in range");
  }
}

checkRange(150); // The number is in the range
checkRange(250); // Number is not in range
